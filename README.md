# blob_detection_in_sunflower_image_using_opencv
in this notebook we made a simple blob detector. we set up the SimpleBlobdetector with default parameters like thresholdStep,Filter by Area,Filter by ColorFilter By Circularity and Filter By Convexity then Detect the blobs and at the end we Draw the blobs
